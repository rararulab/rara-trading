services:
  timescaledb:
    image: timescale/timescaledb:latest-pg16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: rara
      POSTGRES_PASSWORD: rara
      POSTGRES_DB: rara_trading
    volumes:
      - tsdb_data:/var/lib/postgresql/data

volumes:
  tsdb_data:
